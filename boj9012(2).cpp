#include<iostream>
#include<string>
#include <stack>
using namespace std;
//FAILED CODE
// ((()))())( 여도 참으로 표시됨.

/*int main() {

	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int t;
	cin >> t;

	while (t--) 
	{
		int x = 0;
		string arr;
		cin >> arr;
	
		for (char c : arr) {
			if (c == '(') x++;
			else x--;
		}

		if (x == 0) cout << "YES" << '\n';
		else cout << "NO" << '\n';

	}

}*/
